{
  "name": "garden-dashboard-backend",
  "version": "1.0.0",
  "description": "Backend API for garden dashboard - receives sensor data, serves planting schedules, and manages tasks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "init-db": "node scripts/init-db.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
